# --- SIP leg: credentials you created in the Fritz!Box UI -------------------
# Telephony > Telephony Devices > Configure New Device > Telephone > LAN/Wi-Fi (IP phone)
# Both values must be >= 8 characters, and the password must differ substantially
# from the username, or the Fritz!Box refuses to save them.
PHONELLM_SIP_USER=phonellm01
PHONELLM_SIP_PASS=change-this-please

# Use the Fritz!Box's IP, not "fritz.box". ".box" is a public gTLD, so the name
# resolves to a stranger on the internet unless your local DNS answers for it --
# and REGISTER would then carry your SIP username off your LAN. phonellm refuses
# to start if the registrar resolves to a public address.
PHONELLM_SIP_REGISTRAR=192.168.1.1
# PHONELLM_ALLOW_PUBLIC_REGISTRAR=false   # true only for an external SIP provider
# Leave BIND_HOST empty to auto-detect the LAN address used to reach the registrar.
# Set it explicitly on a multi-homed host.
PHONELLM_BIND_HOST=
PHONELLM_BIND_PORT=5060
PHONELLM_RTP_PORT_MIN=16384
PHONELLM_RTP_PORT_MAX=16484

# --- OpenAI Realtime leg ---------------------------------------------------
OPENAI_API_KEY=sk-...
PHONELLM_MODEL=gpt-realtime
PHONELLM_VOICE=alloy

# Server-VAD tuning. Raise these if the log reports "the model interrupted itself":
# that means the handset is echoing the assistant back into the line and the model
# hears its own voice as the caller barging in, cutting replies off mid-sentence.
# PHONELLM_VAD_THRESHOLD=0.5      # 0.0-1.0; higher = less sensitive
# PHONELLM_VAD_SILENCE_MS=500     # how long a pause ends the caller's turn
# PHONELLM_VAD_PREFIX_MS=300
# PHONELLM_INSTRUCTIONS_FILE=/etc/phonellm/instructions.txt

# --- Call behaviour --------------------------------------------------------
PHONELLM_RING_DELAY=2s
PHONELLM_MAX_CALL_TIME=5m
PHONELLM_SILENCE_TIMEOUT=30s

# Milestone 0 gate: echo the caller back to themselves, no LLM, no API key needed.
# Run with this set to true FIRST. If you do not hear yourself, nothing else matters.
PHONELLM_ECHO_TEST=false

# Outbound test call: place one call on startup (once registered) to prove the outbound
# leg without waiting for someone to ring in. Both must be set. Combine with
# PHONELLM_ECHO_TEST=true to test raw audio, or leave it false to have the LLM converse.
OUTBOUND_TEST_CALL=false
OUTBOUND_TEST_NUMBER=
# Optional: a separate prompt for the outbound call. Falls back to the inbound prompt
# (PHONELLM_INSTRUCTIONS[_FILE]) when unset. PHONELLM_OUT_INSTRUCTIONS sets it inline.
# PHONELLM_OUT_INSTRUCTIONS_FILE=./out_instructions.txt

# --- Artefacts (optional) --------------------------------------------------
# Relative paths work for running by hand. Under systemd the unit sets
# StateDirectory=phonellm, so use /var/lib/phonellm/... there instead.
PHONELLM_RECORD_DIR=./recordings
PHONELLM_TRANSCRIPT_DIR=./transcripts

# --- Notification (all optional) -------------------------------------------
PHONELLM_SUMMARY_MODEL=gpt-4.1-mini
# PHONELLM_TELEGRAM_TOKEN=
# PHONELLM_TELEGRAM_CHAT_ID=
# PHONELLM_SMTP_HOST=
# PHONELLM_SMTP_PORT=587
# PHONELLM_SMTP_USER=
# PHONELLM_SMTP_PASS=
# PHONELLM_SMTP_FROM=
# PHONELLM_SMTP_TO=

# PHONELLM_LOG_LEVEL=info
